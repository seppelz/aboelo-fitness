# ============================================
# ABOELO FITNESS - Email Configuration
# ============================================
# Copy this file to .env and fill in your values

# MailerSend API Configuration (RECOMMENDED - bypasses SMTP blocking)
MAILERSEND_API_TOKEN=your-mailersend-api-token  # Get from MailerSend dashboard (starts with mlsn_)
MAILERSEND_API_URL=https://api.mailersend.com   # MailerSend API base URL
EMAIL_FROM=info@aboelo.de                        # Sender email address

# SMTP Server Configuration (Fallback if MailerSend not configured)
# Only needed if NOT using MailerSend
# EMAIL_HOST=smtp.your-provider.de          # e.g., smtp.strato.de or smtp.ionos.de
# EMAIL_PORT=587                            # Usually 587 (STARTTLS) or 465 (SSL)
# EMAIL_SECURE=false                        # false for port 587, true for port 465
# EMAIL_USER=info@aboelo.de                 # Your full email address
# EMAIL_PASSWORD=your-email-password        # Your email password

# Frontend URL (for password reset links)
FRONTEND_BASE_URL=http://localhost:3000    # Development: http://localhost:3000
                                           # Production: https://fitness.aboelo.de

# Contact Form
CONTACT_FORM_TO=info@aboelo.de             # Where contact form emails should go

# Test Email
TEST_EMAIL_RECIPIENT=your-test@email.com   # For testing: npm run test:email

# ============================================
# Other Configuration (if not already set)
# ============================================
JWT_SECRET=your-secret-key-change-in-production
MONGODB_URI=mongodb://localhost:27017/aboelo-fitness
PORT=5000
NODE_ENV=development
